{
  "name": "otr-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Plataforma operativa interna para gestión de tickets y contenido",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "api": "pnpm --filter api dev",
    "web": "pnpm --filter web dev"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
